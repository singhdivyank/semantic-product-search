.DS_Store
.png
load_data.py
*.pdf
Schema.sql

# Python artifacts
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Virtual environments
.venv
venv/
env/

# Local data (JSONL files can be gigabytes — never bake into image)
data/
*.jsonl
*.jsonl.gz
*.parquet

# Secrets
.env
*.pem
*.key

# Git
.git/
.gitignore

# Docker files themselves (already in context)
docker-compose*.yml
.dockerignore

# IDE
.vscode/
.idea/
*.swp

# Test output
htmlcov/
.coverage
coverage.xml

# HuggingFace model cache (mounted as volume, not baked in)
.cache/
 
# Monitoring data (mounted as volume)
monitoring/grafana/dashboards/*.json